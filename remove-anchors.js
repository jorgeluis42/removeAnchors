// create a system to read the string
// once the string is read it has to identify #
//after identifying #, the characters afterwards should be removed


function removeUrlAnchor(url) {
    return url.split('#')[0]
   
   }